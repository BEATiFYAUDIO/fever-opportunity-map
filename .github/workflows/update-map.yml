name: Update Fever Opportunity Map

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  schedule:
    - cron: "0 12 * * *"  # daily at 12:00 UTC

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      TM_API_KEY: ${{ secrets.TM_API_KEY }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt || true
          pip install requests tenacity folium branca pandas numpy python-dateutil

      - name: Debug - verify paths
        run: |
          echo "WORKSPACE=${GITHUB_WORKSPACE}"
          ls -la
          echo "--- scripts ---"
          ls -la scripts || true
          echo "--- docs ---"
          ls -la docs || true

      - name: Build map (pure Python)
        run: |
          set -euo pipefail
          test -f scripts/build_map.py || (echo "scripts/build_map.py missing" && exit 1)
          python scripts/build_map.py

      - name: Ensure .nojekyll
        run: touch docs/.nojekyll

      - name: Commit and push updated artifacts
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/fever_market_opportunity_map.html docs/fever_events.geojson docs/.nojekyll || true
          if ! git diff --cached --quiet; then
            git commit -m "Auto-update map (pure Python)"
            git push
          else
            echo "No changes to commit."
          fi
